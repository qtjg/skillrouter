{
  "name": "skillrouter",
  "version": "0.1.0",
  "description": "The capability router for AI agents. Give your AI the right capabilities at the right time.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "bin": {
    "skillrouter": "./bin/skillrouter.js"
  },
  "files": [
    "bin",
    "dist",
    "src",
    "schemas",
    "examples",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test \"tests/unit/*.test.ts\" \"tests/router/*.test.ts\"",
    "test:unit": "node --experimental-strip-types --test \"tests/unit/*.test.ts\"",
    "test:router": "node --experimental-strip-types --test \"tests/router/*.test.ts\"",
    "test:cli": "node --experimental-transform-types --test \"tests/cli/*.test.ts\"",
    "test:security": "node --experimental-strip-types --test \"tests/unit/security-keys.test.ts\" \"tests/unit/secrets.test.ts\" \"tests/unit/policy.test.ts\"",
    "test:adapters": "node --experimental-strip-types --test \"tests/adapters/*.test.ts\"",
    "benchmark": "node --experimental-strip-types --test \"tests/benchmark/*.bench.ts\"",
    "doctor": "node --experimental-transform-types src/cli/index.ts doctor",
    "prepare": "npm run build"
  },
  "keywords": [
    "ai",
    "agent",
    "skills",
    "mcp",
    "router",
    "capabilities",
    "cli",
    "opencode",
    "gemini",
    "claude"
  ],
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
